name: core
version: 1.0.0
metadata:
  description: core
macros:
  - id: enforce-service-boundaries
    parameters:
      - name: entity
        required: false
        default: service
    body:
      - choir define goal "core-1.0.0"
