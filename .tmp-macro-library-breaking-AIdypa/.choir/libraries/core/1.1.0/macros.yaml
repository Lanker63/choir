name: core
version: 1.1.0
metadata:
  description: core
macros:
  - id: enforce-service-boundaries
    parameters:
      - name: entity
        required: true
    body:
      - choir define goal "core-1.1.0"
