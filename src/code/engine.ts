import { EnforcementContext } from "../core/context.js";
import { Diagnostic } from "../core/types.js";

export function runCode(_context: EnforcementContext, _traceId: string): Diagnostic[] {
  return [];
}